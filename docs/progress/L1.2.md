# L1.2 — Product top bar (D5, D6)
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/ui_common.py` —
  - `header.topbar` (56px, sticky, white, bottom border): the lockup top-left
    links to the Workspace (`/`); a role-gated page switcher
    (Workspace · Curator · Admin); a spacer; the user menu (`#kf-who`).
  - `NAV` is now 3-tuples `(label, path, roles)`; `shell()` renders each link
    with a `data-roles` attribute and `RUNTIME_JS::applyRoleNav()` hides the
    links the current session's roles do not include.
  - **No tenant selector** (D5) and **no narrative subtitle** (D6) — `shell()`
    still accepts a `subtitle` argument for call-site compatibility but never
    renders it.
  - `<title>` is `QualiZeal Knowledge Fabric — <page>`; `favicon-32.png` linked.

## Done-when checks

- Every page carries the 56px lockup bar linking to `/`, the role switcher and
  the user menu; no tenant selector; no subtitle — **PASS**.

## Tests run

`test_brand_shell` asserts the nav paths, the active Workspace link, the title
prefix and the absence of `id="kf-tenant"`. Full suite 296 / 296.

## Blocked

none.
