# L0.1 — Brand kit into the repo
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/static/assets/brand/` (new) — the QualiZeal
  brand kit copied whole:
  - `logo/qualizeal-lockup.png` (1674×204, transparent), `-lockup@1x.png`
  - `logo/qualizeal-mark.png` (512×512, transparent), `-watermark.png`
  - `logo/favicon-{16,32,48,180,512}.png`
  - `tokens.css` (deck-template design tokens), `BRAND.md` (styling rule)
- `knowledge_fabric/surfaces/static/brand/qualizeal-mark.jpg` and
  `-wordmark.jpeg` — **removed** (`git rm`). The demo-era JPEGs had
  white backgrounds; the kit's PNGs are transparent and are the deck
  template's exact files.
- `ci/licence_manifest.json` — new `first_party` licence class
  (`LicenseRef-QualiZeal-Owned`), new `brand-asset` linkage rule, and a
  component recording the brand kit as first-party assets.
- `scripts/build_showcase.py` — `BRAND_SRC` now points at
  `static/assets/brand`; the whole tree is copied (`copytree`) so the
  `logo/` subfolder comes along; the placeholder shell uses the
  transparent lockup PNG in the header, the mark PNG for the watermark,
  and `favicon-32.png`.
- `knowledge_fabric/surfaces/static/README.md` — layout section updated
  to the `assets/brand/` path; records the JPEG removal.
- `tests/test_stage2_ui.py` — brand-serving tests rewritten:
  `test_brand_lockup_served`, `test_brand_mark_served`,
  `test_favicon_served` (all PNG), `test_old_demo_jpegs_gone` (the old
  path now 404s).
- `tests/test_showcase.py` — asserts the lockup + mark PNGs are copied
  and the header references `./assets/brand/logo/qualizeal-lockup.png`.

## Done-when checks

- `/static/assets/brand/logo/qualizeal-lockup.png` returns 200 —
  **PASS** (`test_brand_lockup_served`).
- The old JPEGs are gone — **PASS** (`git rm`;
  `test_old_demo_jpegs_gone` sees a 404).
- PNGs recorded in the licence manifest as first-party assets — **PASS**
  (licence gate green).

## Tests run

`python -m unittest discover -s tests -p 'test_*.py'` → **275 / 275**
(net +2; 0 skipped). Licence gate PASS.

## Deferred

- **Applying the tokens to every surface** (L1.1). L0.1 only lands the
  assets and serves them; the navy consoles are re-skinned to
  `tokens.css` in L1.1. The showcase placeholder already uses the white
  palette and the lockup.

## Blocked

none.
