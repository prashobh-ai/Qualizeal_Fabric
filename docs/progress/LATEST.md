Current: T01 complete — the build spine is in place. FastAPI/Pydantic/uvicorn is the declared runtime with every heavier capability behind an optional extra; uv manages the env and lock; Ruff (line-length 100) and pytest run clean across the whole repository; CI is a four-job pipeline (lint → lite → full → mcp) with the licence gate and corpus policy on every job; the licence manifest feeds a new `scripts/notices.py` that writes `THIRD_PARTY_NOTICES.md`. Console CSS/JS/HTML was extracted to real files under `surfaces/assets/` with every served page proven byte-identical. `make install && make lint && make test` all pass (305 tests). Next: T02.
