Current: F8 complete — the whole product runs on GitHub Pages as an interactive, role-based static demo (landing + chatbot, sign-in, Workspace, Admin with user CRUD, Curator with the feedback queue, Telemetry), driven client-side by a browser-side engine over a baked snapshot; plus a scheduled ingestion-check GitHub Action. New product features: Users & Access (add/remove) and the reader feedback loop (👎 → curator review).
