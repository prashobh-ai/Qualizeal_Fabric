Current: T35–T50 complete — Production on GitHub. The provider is pinned (Sonnet 4.6 / Haiku 4.5 only) and proven on every Actions run (`doctor --require anthropic`, provider line + ledger line in the run summary; keyless is explicit, never a silent mock); every model call lands in the API ledger that feeds Admin → Models. The live GitHub connector writes exact repository facts (commit count from GraphQL `history.totalCount`, PRs, contributors, languages, deployments, workflows) and files one cited document per PR / commit / issue / release / source file; the analysis layer produces symbols, comments, call graph, dependencies with resolved licences, capability evidence lines with the readiness checklist, the repository card and the validated model architecture summary. Documents of every kind convert (Docling, Excel/CSV → SQLite tables, images with OCR + description, Jira live, Confluence). Facts answer the ten repository questions at Level 0 with as-of times; table questions run one SELECT with a row citation; the tool-using agent covers the rest; coverage baking pre-answers what the corpus invites; the ask queue answers via GitHub Issues; ingest / showcase / quality schedules run over the `fabric-data` branch; Curator Repositories / Tables / Insights, Admin Sources / Models and 16 MCP tools surface it; the quality sets gate it (facts 50/50, tables 20/20, images 10/10, jira 10/10, false_answer_rate 0, exact_count_accuracy 1.0); `make demo` shows it end to end. Docs: `docs/progress/T35.md` … `T50.md`.
