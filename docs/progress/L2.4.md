# L2.4 — Compact answer galaxy + Explain overlay
Status: DONE

## Files created/changed

- **`GET /api/galaxy?trace_id=`** (`surfaces/http_api.py`, new) — the activated
  subgraph for one answer, **self-scoped**: an asker may light up only their own
  answers; a curator/admin any trace in the tenant; an unknown/foreign trace
  returns an empty galaxy (not an error) so the rail degrades gracefully.
  - Assembler `_galaxy_for_trace`: activation comes from the answer span's
    persisted `trajectory.{selected, graph_node_keys}`. Node provenance is keyed
    by content hash (not passage id), so activation lights up any entity node
    whose name appears in the retrieved passage text — the reliable "activation
    from retrieved passages" link — plus graph-expansion keys. Returns
    `{nodes[{id,label,type,activated}], edges[{src,dst,relation,activated}], stats}`.
- `ask_ui.py::renderGalaxy` — a from-scratch, dependency-free force galaxy in
  inline SVG: deterministic seed layout + a short spring/repulsion settle,
  camera-fit `viewBox`, activated edges bright / context edges at 0.04, a
  one-shot flash on the lit nodes, and labels only on the most-connected lit
  nodes (a compact rail stays legible).
- `ask_ui.py::openExplain` — **"Why did the AI say this?"** opens the five-node
  Explain overlay: Decision → Sources → Evidence → Document → Page.

## Done-when checks

- The galaxy lights up from the retrieved passages, dims context edges, flashes
  once, fits the frame; Explain shows the five nodes — **PASS** (browser smoke:
  24 nodes, 21 lit, 7 links; explain overlay True).

## Tests run

`test_galaxy_is_self_scoped_and_activates` (own trace lit; foreign trace empty;
401 unauthenticated). Full suite 299 / 299.

## Blocked

none.
