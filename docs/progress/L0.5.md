# L0.5 — Enter submits (D12)
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/ask_ui.py` — the composer keydown handler
  now submits on `Enter` and inserts a newline on `Shift+Enter`
  (was Ctrl/Cmd+Enter). `preventDefault()` stops the newline on submit.
- `tests/test_stage2_ui.py::TestMarkup::test_enter_submits_shift_enter_newlines`
  (new) — asserts the Enter-submit binding is present and the old
  Ctrl/Cmd+Enter binding is gone.

## Done-when checks

- Enter sends, Shift+Enter newlines, in the composer — **PASS**.

## Tests run

Part of the full suite (286 / 286).

## Deferred

- The three F1 static-app composers (Workspace, Admin, Curator) will
  carry the same binding when they are built (L1/L2); the rule is a
  one-liner reused there.

## Blocked

none.
