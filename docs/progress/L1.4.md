# L1.4 — Sign-in page (D8)
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/signin_ui.py` (new) — `SIGNIN_HTML`, served at
  `/signin`: a white card with the lockup, the heading
  "Sign in to QualiZeal Knowledge Fabric", a user id + password form against the
  local IdP, and a disabled SSO placeholder ("available with the corporate
  directory"). The showcase build adds a labelled demo identity picker
  (Asker · Restricted asker · Curator · Admin) under "Showcase sign-in" that
  signs in through the same `/login` endpoint. Reuses `BRAND_CSS`, `RUNTIME_JS`,
  `_FAVICON`, `_LOCKUP`, `_VERSION`, `demo_directory` from `ui_common`.
- `knowledge_fabric/surfaces/http_api.py` — `/signin` route serves `SIGNIN_HTML`.
- `ui_common.py::RUNTIME_JS` — the session lives in memory + `sessionStorage`;
  `logout()` and the role gate redirect to `/signin` when there is no session.

## Done-when checks

- `/signin` is reachable with no session; carries the lockup, the local IdP
  form, the disabled SSO button and the showcase picker — **PASS**.

## Tests run

`TestServedPages::test_signin_page_is_public` (new) and `test_pages_are_served`
(now includes `/signin`). Full suite 296 / 296.

## Blocked

- The real password check arrives with the corporate directory (config only);
  the local IdP accepts the demo users today.
