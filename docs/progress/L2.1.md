# L2.1 — Workspace layout: threads · conversation · rail
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/ask_ui.py` — full rewrite from a single centred
  card to a three-column product surface:
  - **Threads** (300) — per-session conversations in `sessionStorage`
    (`kf.threads`): New chat, click to re-open, the first question names the
    thread; active highlight.
  - **Conversation** — a slim corpus strip (numbers, labels under), the message
    stream (question bubble + answer block), and the composer.
  - **Right rail** (380) — answer galaxy, the card, My usage.
  - `main` is broken out of the centred content column (full-bleed grid,
    fills the height between the top bar and footer); columns scroll
    independently; collapses to 2 / 1 column under 1180 / 900px.
- **Composer**: a mic button (placeholder, disabled until voice), the question
  textarea (auto-grows; Enter sends, Shift+Enter newlines), an answer-language
  pill (Auto-detect · EN · FR · ES · JA), a read-aloud toggle (placeholder),
  and Send.

## Done-when checks

- Three columns render; a new chat starts a thread; the first question titles
  it; the composer sends — **PASS** (browser smoke + `test_page_ids`).

## Tests run

`ASK_IDS` updated to the Workspace ids; full suite 299 / 299. Driven live in
Chromium (sign in → ask → answer) with zero JS errors.

## Blocked

- Threads are per-session (client). Server-side thread persistence is a later
  step if cross-device history is wanted; not needed for the showcase.
