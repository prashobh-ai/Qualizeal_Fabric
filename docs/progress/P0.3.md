# P0.3 — Tenants and users renamed

Every customer-shaped name is replaced by a Q-domain slug; every person name
is replaced by a role slug. No people's names appear anywhere in code, docs,
fixtures, UI or commits.

## Tenants

| Old (removed everywhere) | New slug | Ontology | Corpus |
|---|---|---|---|
| `acme-assurance` | `q-quality` | `quality-assurance` | preloaded |
| `northwind-air` | `q-airlines` | `aviation-ops` | preloaded |
| `meridian-health` | `q-health` | `health` (was `health-provider`) | preloaded |
| — | `q-aerotech` | `aviation-ops` | empty, budget-configured |
| — | `q-assure-claims` | `quality-assurance` | empty |
| — | `q-pharma` | `health` | empty |
| — | `q-devicelab` | `quality-assurance` | empty |
| — | `q-bank` | `quality-assurance` | empty |
| — | `q-assurance` | `quality-assurance` | empty |
| — | `q-cruise` | `aviation-ops` | empty |
| — | `q-retail` | `quality-assurance` | empty |

The extra empty tenants are declared so any script, console or connector can
address them by slug without a code change; bulk upload or a connector pull
seeds them at demo time.

## Users (role-only ids)

| Old id | New id | Roles | Scopes |
|---|---|---|---|
| `asha.asker` | `asker.public` | `asker` | `public` |
| `rana.restricted` | `asker.public` | `asker` | `public` |
| `nia.asker` | `asker.public` | `asker` | `public` |
| `mo.asker` | `asker.public` | `asker` | `public` |
| — (new) | `asker.restricted` | `asker` | `public`, `restricted` |
| `carl.curator` | `curator` | `curator` | `public`, `restricted` |
| `adar.admin` | `admin` | `admin` | `public`, `restricted` |
| `kf-agent` | `qa-agent` | `agent` | `public` |

`DEMO_USERS` is now generated from a single `_ROLE_USERS` template applied
to every tenant, so every Q-* slug ships the same five roles.

## Removed identifiers

`grep -rn "acme-assurance\|meridian-health\|northwind-air\|health-provider\|asha\.asker\|carl\.curator\|adar\.admin\|rana\.restricted\|nia\.asker\|mo\.asker\|kf-agent\|HEALTH_PROVIDER"` returns zero matches across `*.py`, `*.md`, `*.html`, `*.json`, `*.yaml`, `*.yml`, `Makefile`.

## Verification

- `python -m unittest discover -s tests -p 'test_*.py'` → 250 / 250 pass
- `python scripts/demo.py` → 21 sections run green, all with Q-* slugs and
  role-only user ids
- `python scripts/licence_gate.py` → PASS

Every subsequent phase uses the new slugs; the mapping table above is the
migration record.
