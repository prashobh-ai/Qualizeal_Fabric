# L1.3 — Watermark + footer (D7)
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/ui_common.py` —
  - `.qz-watermark` — the QualiZeal mark, 220px, ~4% opacity, fixed bottom-right,
    `pointer-events:none`, behind the content.
  - `.qz-footer` — three fields separated by middots:
    `© QualiZeal. All rights reserved.` · `QualiZeal Knowledge Fabric · Internal`
    · the build version.
  - Both are emitted by `_SHELL` on every console page.
- `dashboard.py` — the same watermark div + footer appended to the telemetry page.

## Done-when checks

- Watermark and footer appear on every page (Workspace, Curator, Admin,
  Telemetry, Sign-in) — **PASS**.

## Tests run

`test_brand_shell` asserts `qz-watermark` and `QualiZeal. All rights reserved.`
on the three consoles. Full suite 296 / 296.

## Blocked

none.
