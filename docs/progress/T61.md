# T61 — Tests

The T51–T60 work is covered by:

- `tests/integration/test_galaxy.py` — the galaxy payload builder (shape,
  activation, halo, node detail, empty/no-graph) and the vendored browser
  module (parses under node, defines KFGalaxy, no external URLs).
- `tests/test_oss_fallback.py` — the open-source composer, its guarded heavy
  deps, the entity-containment guard and the provider label.
- `tests/unit/test_graph_insights.py` — type affinity, Adamic-Adar by hand,
  communities + cohesion, the four-signal blend, surprising bridge, gaps and
  the outline extractor.
- `tests/unit/test_curation_modes.py` — mode resolution, the review queue and
  the timeline (deterministic across orderings after the `(ts, rowid)` fix).
- `tests/integration/test_review_gate.py` — a manual-review document is never
  cited nor discovered, and acceptance makes it answerable.
- `tests/unit/test_token_meter.py` — the phase timer and the token-meter
  overview / definitions.
- `tests/integration/test_mcp.py` — the server lists all 19 governed tools.
- `tests/test_t51_t62_intelligence.py` — the three new MCP tools' contracts and
  the `demo_intelligence` smoke gate.

The licence gate (`tests/test_stage2_licence.py`) accounts for the new guarded
imports (networkx, spacy, tiktoken, transformers) and the vendored vis-network.

## Verification

Full `pytest` green in this session; `ruff` clean; the licence gate passes.
