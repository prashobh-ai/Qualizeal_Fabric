# L1.5 — Name / words audit (D11)
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/ask_ui.py` —
  - the why-card no longer shows the raw `level`/`tier`; it renders the level as
    one of the four reader words via a `levelWord()` map
    (`lookup→Look it up`, `fast→Quote it`, `reason→Summarise it`,
    `escalation→Reason about it`; `reasoning:*→Reason about it`; the special
    outcomes read as sentences), coloured by the fixed brand level colours.
  - dropped the "Tier / level" fact tile; the trace tile is labelled "Trace".
- `tests/test_stage2_ui.py::TestWordsAudit` (new, L1.5 / D11) — a
  `_visible_text()` extractor (text nodes + visible attributes + prose-like JS
  string literals, excluding `<style>`/`<script>` machinery, ids, selectors and
  `console.*`) drives four checks over the five served pages:
  - no forbidden word visible: `tenant`, `tier`, `trajectory`, `kf-mock`,
    `RRF`, `pgvector`, `token ledger`, `console`;
  - `QualiZeal` spelt correctly everywhere (no `Qualizeal`/`QUALIZEAL`/`Quali Zeal`);
  - no level number in visible text (`L2`, `Level 3`);
  - the Ask console surfaces all four reader level words.
- `ANSWER_FIELDS` in the contract test drops `tier`/`level` — the reader-facing
  page reads the level via `why.level_name` only.

## Done-when checks

- Every served page's visible text is free of internal / storage / mis-spelt
  words and of level numbers; level words present — **PASS**.

## Tests run

`TestWordsAudit` (4 tests) + the updated contract/served tests. Full suite
296 / 296.

## Blocked

none.
