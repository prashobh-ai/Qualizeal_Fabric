# L2.2 — The answer: inline citation chips, quotes, declines
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/ask_ui.py` —
  - `[n]` markers render as inline **citation chips** `Title · p.14`
    (`withChips`); clicking one opens the **page viewer** drawer with the cited
    passage highlighted (`<mark>`) plus coordinate + passage id. Until document
    conversion lands, the highlighted passage stands in for the page (noted in
    the drawer).
  - **Verbatim quotes render as quotes**: a "Quote it" level answer is styled
    as a left-ruled italic blockquote.
  - **Declines** read as a plain sentence — *"There isn't enough evidence in
    the fabric to answer that."* — plus the reason (clarify shows the
    clarify-back question).

## Done-when checks

- Citation chips open the page viewer; quotes render as quotes; declines read
  the fixed sentence — **PASS** (browser smoke: chips clickable, page viewer
  opened True).

## Tests run

Full suite 299 / 299; the served answer still carries every citation field
(`document_title`, `coordinate_render`, `snippet`, `passage_id`).

## Blocked

- Full-page rendering waits on document conversion (Docling); today the cited
  passage is shown highlighted.
