# L2.3 — The card (replaces the three Stage-2 boxes)
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/ask_ui.py::card()` — one right-rail card, one row
  per fact, in the spec order. First four rows open; the rest under **Details**:
  1. **Answered by** — the reader-facing level word.
  2. **Why** — one sentence (`why.explain`), reader-safe wording.
  3. **Model** — model name, or "demo model" in mock mode.
  4. **Moved levels** — "Yes — escalated after a confidence check" / "No".
  5. Relationships (count · hops · documents, from the galaxy) · **Sources**
     (found / cited) · **Trust** (0–100 + five signal bars + grounding) ·
     Language · **Tokens** (in / out / cache read —) · **Cost** (this · top ·
     saved) · Cache · Timing · Complexity + dataset · **Authoritative source**
     (badge + conflict note) · **Trace** (the id lives behind the link).
- **Backend surfacing** (`answer/service.py`) — the five grounding signals
  (`why.signals`: retrieval, semantic, coverage, agreement, resolvable) and the
  retrieved count (`why.retrieved`) are now on the answer, so Trust and Sources
  are real, not derived guesses.
- **D11 runtime fix** (`answer/selector.py::_explain`) — the "Why" text no
  longer says "Routed to the fast tier"; it reads "Quoted the most relevant
  passages…", "Summarised across the evidence…", etc. No internal routing word
  reaches the reader. Guarded by a new runtime test.

## Done-when checks

- Every row populates from a real answer; four rows open, the rest under
  Details; the trace id is hidden behind the Trace link — **PASS** (browser
  smoke).

## Tests run

`test_ask_answer_card_fields` asserts `why.signals` (the five keys) and
`why.retrieved ≥ cited`; `test_runtime_why_text_has_no_internal_words` greps
`why.explain` across three questions. Full suite 299 / 299.

## Blocked

- The five-stage token waterfall arrives with voice (F6); "cache read" shows —.
