# P1.1 — Brand, tokens, fonts
Status: DONE (with deferrals)

## Files created/changed

- `knowledge_fabric/surfaces/static/README.md` (new) — declares layout and deferrals
- `knowledge_fabric/surfaces/static/brand/qualizeal-mark.jpg` (new, copied verbatim from the demo)
- `knowledge_fabric/surfaces/static/brand/qualizeal-wordmark.jpeg` (new, copied verbatim from the demo)
- `knowledge_fabric/surfaces/http_api.py` — added `_STATIC_ROOT`, `_STATIC_TYPES`, `_serve_static`, and the `/static/*` route with a `Cache-Control: public, max-age=3600` header. Path traversal is refused (`realpath` must live inside `_STATIC_ROOT`).
- `knowledge_fabric/surfaces/ui_common.py::BRAND_CSS` — added the QualiZeal design tokens (`--qz-blue`, `--qz-pink`, `--qz-violet`, `--qz-cyan`, `--qz-canvas` + shades) lifted from the demo `styles/main.css`; kept the existing dark-shell tokens verbatim so every current console still renders correctly.
- `knowledge_fabric/surfaces/ui_common.py::BRAND_CSS` — added the navy-canvas rule scoped to `.galaxy, .health-ring` only (per §1: "light chrome for consoles and dashboard, the navy canvas only for the galaxy panel and the health ring").
- `tests/test_stage2_ui.py::TestMarkup::test_brand_tokens_present` (new) — every served page carries the four `--qz-*` brand tokens and the `--qz-canvas` navy variables plus the `.galaxy, .health-ring` scoping rule.
- `tests/test_stage2_ui.py::TestStaticAssets` (new) — mark and wordmark served with `image/jpeg` and a public `Cache-Control`; a missing asset returns 404; three path-traversal payloads all 404.

## Done-when checks

- Two brand files copied — **PASS** (`ls surfaces/static/brand/` shows both).
- `/static/*` route serves from `surfaces/http_api.py` with cache headers — **PASS** (`Cache-Control: public, max-age=3600` on every hit; `TestStaticAssets.test_brand_mark_served`).
- Design tokens added to the shared stylesheet — **PASS** (`test_brand_tokens_present`).
- Navy canvas applied to `.galaxy` and `.health-ring` only — **PASS** (rule present; the rest of the shell keeps the existing dark chrome).
- No `http://` or `https://` script/link tags in served HTML — **PASS** (`test_zero_external_dependencies`, pre-existing; still green).

## Tests run

`python -m unittest discover -s tests -p 'test_*.py'` → **255 / 255** (5 new; 0 skipped).

## Deferred

- **vis-network bundle.** The P1.3 galaxy panel is specified to use `vis-network` (Apache-2.0 / MIT dual) vendored into `surfaces/static/vendor/`. This session's outbound HTTPS proxy denies every allowed JS CDN (cdnjs, jsdelivr, unpkg, raw.githubusercontent). Recorded in `surfaces/static/README.md`; P1.3 will ship a stdlib-only inline-SVG force-directed renderer as a functional stand-in until the operator drops the real bundle into `surfaces/static/vendor/vis-network.min.js`. This deferral is scoped: it does not violate any invariant, and the API surface stays identical.
- **Inter font faces.** OFL Inter woff2 files are also blocked at fetch time; served HTML falls back to the existing system stack (`Inter, system-ui, Segoe UI, Roboto, sans-serif`). Drop the woff2 files into `surfaces/static/fonts/` and uncomment the `@font-face` block in `ui_common.py` when they become available.

## Blocked

none — every done-when check passes on the shipped stdlib runtime; the two
deferrals are asset vendoring that can happen offline without any code change.
