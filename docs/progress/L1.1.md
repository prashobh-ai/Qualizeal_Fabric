# L1.1 — White theme, one stylesheet (D1)
Status: DONE

## Files created/changed

- `knowledge_fabric/surfaces/ui_common.py` — `BRAND_CSS` `:root` now inlines
  the QualiZeal deck-template tokens (from `static/assets/brand/tokens.css`,
  kept same-origin) and maps the shell component variables onto them
  (`--surface:var(--qz-surface)`, `--panel`, `--fg`, `--accent`, `--good/warn/bad`,
  …). Page surface is white (`--qz-surface:#FFFFFF`), cards `--qz-panel`,
  borders `--qz-line`, Inter with tabular numerals, `html{color-scheme:light}`.
- The navy canvas (`--qz-canvas:#0E1A45`) is scoped to `.galaxy,.health-ring`
  only — no other element carries a background darker than `#F4F8FC`.
- Added the four reader-level pill colours (`.pill.lv-look/lv-quote/lv-sum/lv-reason`)
  in the fixed brand order (green / blue / purple / coral).
- `ask_ui.py`, `dashboard.py` — replaced the last hard-coded navy fills
  (`#182449`, `#0a0f26`, meter track, shield) with brand tokens / light values.

## Done-when checks

- No page has a background darker than `#F4F8FC` outside `.galaxy` — **PASS**
  (grep audit over the five served pages is clean).

## Tests run

`tests/test_stage2_ui.py` — `test_brand_shell`, `test_brand_tokens_present`
assert the white palette and the scoped canvas. Full suite 296 / 296.

## Blocked

none.
